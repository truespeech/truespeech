{
  "name": "truespeech",
  "version": "0.1.0",
  "description": "True Speech runtime — a language for codifying the data analytics practice on top of semantic layers.",
  "type": "module",
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsx --test test/*.test.ts"
  }
}
